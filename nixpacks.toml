providers = ["python", "nodejs"]

[variables]
PYTHONPATH = "/app"
PYTHON_VERSION = "3.11"
NODE_VERSION = "20"

[start]
cmd = "cd backend && python3 main.py"

[build]
cmds = ["cd backend && pip install -r requirements.txt"]